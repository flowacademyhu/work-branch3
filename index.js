const sum = (a, b) => { // writen by Daniel
  return a + b;
};

const sub = (a, b) => { // writen by Daniel
  return a - b;
};

const mul = (a, b) => { // written by gy
  return a * b;
};

const div = (a, b) => { // written by gy
  return a / b;
};

module.exports = { // written by Iza
  sum,
  sub,
  mul,
  div
};
