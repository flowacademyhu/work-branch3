const summary = (a, b) => {
	return a+b;

module.export = {
    summary,
    div,
    sub
}

const sub = (a, b) => { // sub new  for team
  return a - b;
};

const div = (a,b) => {  // div for team projekt branch3 madeByKZ
    return a/b;
    };